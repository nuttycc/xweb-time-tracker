/**
 * 类型定义统一导出文件
 * 集中导出所有类型定义，方便其他模块引用
 *
 * 职责范围：
 * - 导出通用接口定义
 * - 导出类型别名
 * - 导出枚举类型
 *
 * 功能边界：
 * ✅ 包含：接口定义、类型别名、枚举、泛型类型
 * ❌ 不包含：具体实现、业务逻辑、数据操作
 *
 * 依赖关系：
 * - 依赖：无（基础类型层）
 * - 被依赖：所有需要类型定义的模块
 *
 * TODO:
 * - [ ] 定义追踪相关类型
 * - [ ] 定义查询相关类型
 * - [ ] 定义存储相关类型
 * - [ ] 定义通用工具类型
 */

// 暂时导出空对象，避免构建错误
export {};
