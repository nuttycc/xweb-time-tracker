/**
 * 常量统一导出文件
 * 集中导出所有常量模块，方便其他模块引用
 *
 * 职责范围：
 * - 导出应用配置常量
 * - 导出事件类型常量
 * - 导出存储相关常量
 * - 导出过滤规则常量
 * - 导出时间相关常量
 *
 * 功能边界：
 * ✅ 包含：常量值、枚举定义、默认配置
 * ❌ 不包含：可变配置、运行时计算值、业务逻辑
 *
 * 依赖关系：
 * - 依赖：无（基础常量层）
 * - 被依赖：所有需要常量的模块
 */

// 时间相关常量
export * from './time';

// 配置相关常量
export * from './config';

// 事件相关常量
export * from './events';

// 存储相关常量
export * from './storage';

// 过滤相关常量
export * from './filters';
